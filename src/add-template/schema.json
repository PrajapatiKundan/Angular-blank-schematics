{
    "$schema": "http://json-schema.org/schema",
    "id": "SchematicsAddTemplate",
    "title": "My Template Schema",
    "type": "object",
    "properties": {
      "name": {
        "description": "The name of the Template.",
        "type": "string",
        "$default": {
            "$source": "argv",
            "index": 0
        },
        "x-prompt": "What is the of template? "
      }
    },
    "required": [
      "name"
    ]
}